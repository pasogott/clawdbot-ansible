# OpenClaw Ansible Inventory Sample

all:
  children:
    openclaw_servers:
      hosts:
        # Example 1: Simple IP address
        192.168.1.100:

        # Example 2: Hostname with specific variables
        my-claw-server:
          ansible_host: 192.168.1.101
          ansible_user: admin
          # Override default variables for this host
          openclaw_install_mode: release

  # Global variables for all OpenClaw servers
  vars:
    # SSH Public Keys for the 'openclaw' user (Optional)
    # If set, these keys will be added to ~/.ssh/authorized_keys
    # openclaw_ssh_keys:
    #   - "ssh-ed25519 AAAAC3Nz..."

    # Optional VPN provider: "tailscale", "netbird", or "" for none
    # vpn_provider: "netbird"

    # Tailscale Auth Key (when using Tailscale)
    # tailscale_authkey: "tskey-auth-..."

    # NetBird Setup Key (when using NetBird)
    # netbird_setup_key: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
    # netbird_management_url: "https://netbird.example.com"  # self-hosted only
